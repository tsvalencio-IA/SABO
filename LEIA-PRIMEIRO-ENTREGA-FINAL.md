# ENTREGA FINAL

Este ZIP já está com o GLB enviado por você instalado como arquivo principal.

## Arquivo principal
`assets/modelos/sabo-05801.glb`

## Tamanho do GLB usado nesta entrega
Aproximadamente 7.69 MB.

## Regras do Realtime Database
Use o arquivo:

`REGRAS-REALTIME-DATABASE-ABERTO-TESTE.json`

Conteúdo:

```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

## Observação
Também deixei um backup do GLB otimizado anterior em:

`assets/modelos/backup/sabo-05801-otimizado-anterior-3mb.glb`

Se o novo GLB ficar pesado no celular, você pode voltar esse backup renomeando ele para:

`sabo-05801.glb`
